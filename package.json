{
  "name": "create-aws-credentials",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "package": "npm run build && ncc build ./build/index.js -o ./dist && ncc build ./build/cleanup.js -o ./dist/cleanup"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@types/node": "^20.9.3"
  }
}
